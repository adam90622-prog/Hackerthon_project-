# 의사결정 로그 — [04] 다이버즈 SNS 콘텐츠 3종 생성

이 파일에 작업 중 내린 중요한 결정을 기록하세요.
"이 결정을 decisions.md에 기록해줘"라고 입력하면 Claude가 자동으로 추가합니다.

---

## 형식
**[시간] 결정 제목**
- 결정 내용:
- 선택한 이유:
- 대안으로 고려했던 것:

---
(여기에 기록이 추가됩니다)

**[2026-07-01] keywords_required 결측 보완 — id 12**
- 결정 내용: id 12 ("국시 끝나고 우리가 하고 싶은 것들", 간호학생) keywords_required = "국시,면허,RN"
- 선택한 이유: 1순위(topic 내 용어 매칭): 국시 / 2순위(ref_event '간호사 국가시험 합격발표' -> industry-news.md '간호사 국가시험 (국시)' 문단, topic tense=post): 후보=RN, SBAR, 프리셉터 / 채택=RN, SBAR / 수동 판단(override) 적용 전 자동 결과: 국시, RN, SBAR / 수동 판단(override) 최종: 국시, 면허, RN
- 대안으로 고려했던 것: 규칙 1~3의 각 단계 후보 전체 (위 근거 참고), ref_event='간호사 국가시험 합격발표'

**[2026-07-01] 중복 topic 플래그 — DUP01**
- 결정 내용: "실습 나가서 듣는 웃픈 한마디 모음" — id 11(간호학생), id 17(경력RN) 를 DUP01로 플래그, 관점 차별화 판단 부여
- 선택한 이유: 간호학생 행 = '당하는' 실습생 1인칭 시점(선배·프리셉터에게 듣는 말), 경력RN 행 = '돌아보는' 선배 시점(후배 시절 회상 + 지금 후배에게 하는 말)으로 관점을 분리
- 대안으로 고려했던 것: topic 문자열 자체를 타깃별로 다르게 바꾸는 방안 — 이번 정제 단계에서는 원문 topic은 유지하고 관점 판단만 남김 (콘텐츠 생성은 다음 턴)


**[2026-07-01] 범주값·구분자 오타 정규화**
- id 32 [tone]: "유모형" -> "유머형" (오타 추정(편집거리 1): '유모형' -> '유머형')
- id 33 [tone]: "정보 형" -> "정보형" (공백 제거: '정보 형' -> '정보형')
- id 34 [target_audience]: "경력알엔" -> "경력RN" (표기 치환: '경력알엔' -> '경력RN')
- id 35 [target_audience]: "신규 RN" -> "신규RN" (공백 제거: '신규 RN' -> '신규RN')
- id 36 [keywords_required]: "인계;태움;SBAR" -> "인계,태움,SBAR" (구분자/공백 정규화)
- id 37 [keywords_required]: "실습,프리셉터,라운딩," -> "실습,프리셉터,라운딩" (구분자/공백 정규화)
- id 38 [keywords_required]: "RN,,LPN,OCS" -> "RN,LPN,OCS" (구분자/공백 정규화)
- id 39 [keywords_required]: " 3교대 , 콜벨 , 인계 " -> "3교대,콜벨,인계" (구분자/공백 정규화)
- 선택한 이유: tone/target_audience는 채널 로직·타깃 매칭에 쓰이는 고정 범주라 오타·공백이 있으면 이후 단계가 깨짐. keywords_required는 세미콜론/중복콤마/트레일링콤마가 섞여 있어 콤마 기준으로 통일
- 대안으로 고려했던 것: 미확인 값(편집거리 2 이상)은 임의로 표준값에 끼워맞추지 않고 review_flag로만 표시해 수동 확인을 유도

**[2026-07-01] 완전 중복 행 제거**
- id 45 ("나이트 근무 버티는 법") 제거 — id 44와 topic·tone·target·keywords·platform·ref_event 전부 동일
- id 47 ("실습 후기 모음") 제거 — id 46와 topic·tone·target·keywords·platform·ref_event 전부 동일
- 선택한 이유: 전 필드가 동일한 행은 동일 콘텐츠를 중복 생성하게 되어 낭비이자 데이터 오류로 판단
- 대안으로 고려했던 것: 두 행 모두 유지 후 콘텐츠 생성 단계에서 중복 제거 — 정제 단계에서 미리 제거하는 쪽이 하위 단계 낭비를 줄여 채택

**[2026-07-01] topic 결측 플래그**
- id 50: topic이 비어 있어 규칙 1(topic 매칭)·규칙 2(ref_event 연결) 적용 불가
- 선택한 이유: topic 없이 keywords_required만으로 주제를 역으로 지어내는 것은 원문 왜곡 위험이 있어 자동 보완하지 않고 review_flag("topic 결측")로 표시, 콘텐츠 생성 대상에서 제외
- 대안으로 고려했던 것: keywords_required 기반으로 topic을 추정 생성 — 근거 데이터가 아닌 추측이라 기각

**[2026-07-01] keywords_required 결측 보완 — id 27**
- 결정 내용: id 27 ("주사 실수 줄이는 법", 신규RN) keywords_required = "바이탈,감염관리,프리셉터"
- 선택한 이유: 3순위(동일 target_audience 저빈도 보충, 빈도 낮은 순): 바이탈(형제행 1회), 감염관리(형제행 1회), 프리셉터(형제행 2회)
- 대안으로 고려했던 것: 규칙 1~3의 각 단계 후보 전체 (위 근거 참고), ref_event='(없음)'

**[2026-07-01] keywords_required 결측 보완 — id 28**
- 결정 내용: id 28 ("실습 마지막 날 소감", 간호학생) keywords_required = "RN,LPN,SBAR"
- 선택한 이유: 3순위(동일 target_audience 저빈도 보충, 빈도 낮은 순): RN(형제행 1회), LPN(형제행 1회), SBAR(형제행 1회)
- 대안으로 고려했던 것: 규칙 1~3의 각 단계 후보 전체 (위 근거 참고), ref_event='(없음)'

**[2026-07-01] keywords_required 결측 보완 — id 29**
- 결정 내용: id 29 ("경력직 첫 출근 긴장 포인트", 경력RN) keywords_required = "라운딩,번아웃,듀티표"
- 선택한 이유: 3순위(동일 target_audience 저빈도 보충, 빈도 낮은 순): 라운딩(형제행 1회), 번아웃(형제행 2회), 듀티표(형제행 2회)
- 대안으로 고려했던 것: 규칙 1~3의 각 단계 후보 전체 (위 근거 참고), ref_event='(없음)'

**[2026-07-01] keywords_required 결측 보완 — id 30**
- 결정 내용: id 30 ("나이트 근무 루틴 브이로그", 신규RN) keywords_required = "데이/이브닝/나이트,5R,RN"
- 선택한 이유: 1순위(topic 내 용어 매칭): 데이/이브닝/나이트 / 3순위(동일 target_audience 저빈도 보충, 빈도 낮은 순): 5R(형제행 2회), RN(형제행 2회)
- 대안으로 고려했던 것: 규칙 1~3의 각 단계 후보 전체 (위 근거 참고), ref_event='(없음)'

**[2026-07-01] keywords_required 결측 보완 — id 31**
- 결정 내용: id 31 ("국시 합격자 인터뷰", 간호학생) keywords_required = "국시,인계,바이탈"
- 선택한 이유: 1순위(topic 내 용어 매칭): 국시 / 3순위(동일 target_audience 저빈도 보충, 빈도 낮은 순): 인계(형제행 1회), 바이탈(형제행 1회)
- 대안으로 고려했던 것: 규칙 1~3의 각 단계 후보 전체 (위 근거 참고), ref_event='(없음)'

**[2026-07-01] keywords_required 형식 재판단 — id 30 (복합 표기 → 단일 용어)**
- 결정 내용: id 30 ("나이트 근무 루틴 브이로그", 신규RN) keywords_required "데이/이브닝/나이트,5R,RN" → **"나이트,5R,RN"** 로 정정 (MANUAL_OVERRIDES["30"]로 고정, 재실행해도 동일 결과)
- 선택한 이유: 1순위 자동 로직은 topic에 등장한 alias "나이트"를 찾았지만 결과에는 그 alias가 속한 용어사전 원형(복합 표기) "데이/이브닝/나이트"를 채택했음. keywords_required의 다른 모든 값은 전부 단일 용어인데 이 값만 슬래시 결합 문자열이라 콘텐츠 본문에 그대로 넣기 어렵고 validate_content.py의 키워드 반영 체크에서 실패할 가능성이 높음. topic="나이트 근무 루틴 브이로그"는 3교대 전체가 아니라 그중 나이트 근무 하나만을 다루므로, 실제로 매칭된 alias 그대로 "나이트"(단일 용어)를 채택 — "3교대"(교대근무 체계 전체를 가리키는 더 넓은 용어)보다 topic의 구체적 소재(나이트 근무 하나)에 더 자연스럽게 맞음
- 대안으로 고려했던 것: "3교대"로 대체 — topic이 특정 교대(나이트)만 다루는데 3교대 전체를 가리키는 용어라 범위가 넓어 기각. "데이/이브닝/나이트" 원형 유지 — 다른 행과의 형식 일관성(단일 용어) 및 콘텐츠 생성·검증 단계 호환성 문제로 기각

**[2026-07-01] 중복 topic 플래그 — DUP01**
- 결정 내용: "선배가 알려준 인계 꿀팁 모음" — id 48(신규RN), id 49(경력RN) 를 DUP01로 플래그, 관점 차별화 판단 부여
- 선택한 이유: 신규RN 행 = 갓 배운 인계 꿀팁을 현장에 처음 적용하는 '실행자' 시점, 경력RN 행 = 후배에게 인계 꿀팁을 전수하는 '전달자' 시점으로 관점을 분리
- 대안으로 고려했던 것: topic 문자열 자체를 타깃별로 다르게 바꾸는 방안 — 이번 정제 단계에서는 원문 topic은 유지하고 관점 판단만 남김 (콘텐츠 생성은 다음 턴)


**[2026-07-01] keywords_required 결측 보완 — id 12**
- 결정 내용: id 12 ("국시 끝나고 우리가 하고 싶은 것들", 간호학생) keywords_required = "국시,RN,SBAR"
- 선택한 이유: 1순위(topic 내 용어 매칭): 국시 / 2순위(ref_event '간호사 국가시험 합격발표' -> industry-news.md '간호사 국가시험 (국시)' 문단, topic tense=post): 후보=RN, SBAR, 프리셉터 / 채택=RN, SBAR
- 대안으로 고려했던 것: 규칙 1~3의 각 단계 후보 전체 (위 근거 참고), ref_event='간호사 국가시험 합격발표'

**[2026-07-01] 중복 topic 플래그 — DUP01**
- 결정 내용: "실습 나가서 듣는 웃픈 한마디 모음" — id 11(간호학생), id 17(경력RN) 를 DUP01로 플래그, 관점 차별화 판단 부여
- 선택한 이유: 간호학생 행 = '당하는' 실습생 1인칭 시점(선배·프리셉터에게 듣는 말), 경력RN 행 = '돌아보는' 선배 시점(후배 시절 회상 + 지금 후배에게 하는 말)으로 관점을 분리
- 대안으로 고려했던 것: topic 문자열 자체를 타깃별로 다르게 바꾸는 방안 — 이번 정제 단계에서는 원문 topic은 유지하고 관점 판단만 남김 (콘텐츠 생성은 다음 턴)

**[2026-07-01] keywords_required 결측 보완 재판단 — id 12 (인덱스 기반 → 국면 기반으로 수정)**
- 결정 내용: id 12 ("국시 끝나고 우리가 하고 싶은 것들", 간호학생) keywords_required "국시,RN,SBAR" → **"국시,나이팅게일 선서,프리셉터"** 로 정정 (context/nursing-calendar.md 추가 반영에 따른 재판단, MANUAL_OVERRIDES["12"]로 고정해 재실행 시에도 동일 결과 보장)
- 무엇이 바뀌었나: 2순위 후보였던 RN·SBAR를 모두 기각하고, 3순위(간호학생 형제행 저빈도 보충)에서 나이팅게일 선서·프리셉터를 새로 채택. 1순위(국시)는 유지.
- 선택한 이유(국면 기반 판단, 인덱스 기반 아님):
  1. ref_event 매칭(1순위 자료 industry-news.md): "간호사 국가시험 합격발표" → industry-news.md "간호사 국가시험 (국시)" 문단과 매칭. 이 문단 전체가 "합격발표 시점: 간호학생→신규RN 전환기... 입사 전 약어·SBAR 미리보기, 프리셉터십 안내"로, **합격발표 이후(취업 준비) 국면**만 서술하고 있음을 확인.
  2. topic의 국면 판단: "국시 끝나고"는 시험 응시가 종료된 시점을 가리키는 표현이며, 관용적으로 "합격 여부를 알기 전, 결과를 기다리는 기간"을 뜻한다(신규RN 전환·SBAR·프리셉터십 같은 취업 준비 활동을 가리키는 표현이 아님). 즉 이 topic은 ref_event(합격발표)의 **이전(대기) 국면**을 다룬다.
  3. 2순위 보강(2순위 자료 nursing-calendar.md, industry-news.md에 없는 세부 일정만 사용): "간호사 국가시험(제66회)" 항목의 시행 2026.1.23(금) vs 합격자발표 2026.2.13(금) — 약 3주의 공백이 있어 "시험 종료"와 "합격발표"가 명확히 분리된 별개 국면임을 뒷받침. industry-news.md와 충돌 없음(세부 일정만 보충).
  4. 따라서 industry-news.md 국시 문단이 서술하는 RN(용어사전 tense=post로 원래도 제외 대상)·SBAR·프리셉터십(둘 다 tense=neutral이라 지난번엔 필터를 통과함)은 전부 "발표 이후" 전용 문맥에서만 언급된 용어이므로, "발표 이전 대기" 국면인 이 topic에는 채택하지 않음. 이것이 지난번 로직의 오류 지점 — neutral 태그 용어를 국면 대조 없이 텍스트 등장 순서로만 뽑아버린 것.
  5. 2순위 후보가 전부 국면 불일치로 제외되어, 3순위(동일 target_audience=간호학생 형제행 저빈도 보충, 빈도 낮은 순, id 07~11 기준)로 보강: 나이팅게일 선서(형제행 1회), 프리셉터(형제행 1회).
  6. "27세 신입간호사 '태움' 사망 사건"(nursing-calendar.md B. 비정기 이슈) 항목은 이 topic·ref_event와 무관하며 판단에 전혀 사용하지 않음 — 금지 항목 확인.
- 대안으로 고려했던 것: 기존 값(국시,RN,SBAR) 유지 — 국면 대조 없이 뽑힌 값이라 기각. RN만 제외하고 SBAR·프리셉터십은 유지 — 둘 다 문맥상 "발표 이후" 전용 문장에서만 등장해 근거가 약해 기각.
- 재현성(인덱스 기반이 아님을 보이는 한 줄 근거): 이 판단은 "ref_event가 가리키는 이벤트의 두 하위 시점(시행일 vs 발표일) 중 topic이 어느 쪽 이전/이후에 있는가"라는 고정된 비교 규칙으로 후보를 채택/기각하며, 후보가 본문에서 몇 번째로 등장하는지는 결과에 전혀 영향을 주지 않는다 — 따라서 동일한 topic·ref_event 조합이 다시 들어와도 항상 같은 국면 판단과 같은 키워드가 나온다.

**[2026-07-01] keywords_required 최종 확정(재판단 중단) — id 12**
- 결정 내용: id 12 ("국시 끝나고 우리가 하고 싶은 것들", 간호학생) keywords_required를 "국시,나이팅게일 선서,프리셉터"에서 **"국시,면허,RN"** 으로 직접 확정. MANUAL_OVERRIDES["12"]도 동일 값으로 갱신해 재실행 시 이 값이 그대로 재현되도록 고정.
- 선택한 이유: 반복 재실행 시 값이 계속 달라지는 재현성 문제가 확인되어, 더 이상 규칙 1~3 재판단을 거치지 않고 "국시 합격 발표 후" 국면에 맞는 용어(면허·RN)로 최종 확정하라는 지시에 따름. 재판단(국면 근거 재검토)은 수행하지 않았음 — 이 항목은 판단 로그가 아니라 확정값 기록임.
- 참고: 직전 항목(위)에서는 "발표 이전(대기)" 국면 판단에 따라 나이팅게일 선서·프리셉터를 채택했으나, 이번 확정으로 그 판단은 대체됨. 두 판단의 차이(대기 국면 vs 발표 후 국면)는 기록으로 남기되, 최종 채택값은 이번 항목의 "국시,면허,RN".
- 재현성 조치: scripts/clean_seeds.py의 MANUAL_OVERRIDES["12"]를 ["국시","면허","RN"]으로 갱신 — 이후 스크립트를 몇 번 재실행해도(자동 rule1~3 결과와 무관하게) 항상 이 값으로 덮어써지므로 더 이상 재실행마다 값이 달라지지 않음.

**[2026-07-01] 환자안전주간 시기 표현 수정 — 시드 #03 뉴스레터**
- 결정 내용: 시드 #03 뉴스레터 도입부 "매년 9월, 환자안전주간이 돌아오면..." → "환자안전주간이 돌아오면 병동에서는 어김없이..."로 수정, 특정 월(9월) 확정 서술 제거
- 선택한 이유: context/nursing-calendar.md 확인 결과 WHO 지정 "세계 환자안전의 날"은 9월 17일로 확정이나, 국내 병원의 "환자안전주간" 정확한 날짜는 "병원별로 상이·미확인"(같은 파일 하단 "검색했지만 출처를 못 찾아 제외한 항목" 목록에도 재확인됨). 특정 월을 확정 서술하면 미확인 정보를 사실처럼 전달하게 되어 제거. 글자수 보정을 위해 "어김없이"를 추가해 546자→551자로 재조정(권장범위 550~650자 하한 충족)
- 대안으로 고려했던 것: "매년" 표기만 남기고 "9월"만 삭제 — 문제의 핵심이 "특정 월 확정"이므로 "9월" 삭제만으로도 충분하나, 더 자연스러운 문장을 위해 전체 도입구를 재작성
- 자체 점검: industry-news.md·nursing-calendar.md의 "미확인"·"예정(변동 가능)" 표시 항목(신규간호사 입사 시즌, 간호대학 임상실습 시즌, 간호사 1인당 적정환자수 법제화 개정안, 27세 신입간호사 태움 사망 사건 등)을 18행 전체와 대조 — 시드 #15·#16의 인력기준·처우개선 언급은 "논의가 이어지고 있다"/"목소리가 크다" 식으로 이미 정확히 헤지되어 있어 추가 수정 없음. 시드 #03 외 추가 수정 필요 항목 없음으로 판단

**[2026-07-01] 브랜드보이스 3종 규칙 정합화 — 18행 전체**
- 결정 내용: output_content_set.md 18개 시드의 카드뉴스·카카오·뉴스레터 전체에 아래 3가지를 적용
  1. 호칭: "신규 RN"/"간호학생"/"경력RN" 등 기존 기능적 지칭을 유지한 채 "선생님" 존칭을 병기(세트당 최소 1회, 대부분 카드뉴스 CTA 또는 뉴스레터 도입부에 배치)
  2. 감성 이모지: 카드뉴스 4장 중 1~2장, 뉴스레터 1~2곳에 톤별 팔레트(공감형 🤍😊🥲 / 정보형 😊💪 / 유머형 😆🙈😅)로 삽입. 카카오메시지는 채널 특성상 이모지 미적용
  3. 증언 인용 포맷("~병원 n년차" 등 특정 인물 귀속)은 도입하지 않고, 기존에 일부 콘텐츠에 있던 "선배들의 이야기를 들어보면" 익명 집단 화법을 18건 전체로 확장 적용(누락된 시드에 자연스럽게 1문장씩 추가)
- 선택한 이유: data/nursevillage_content_seeds_cleaned.csv에 실제 증언·인터뷰 데이터가 없어 구체적 인물 귀속 인용은 지어낸 가짜 인용이 됨. 익명 집단 화법은 이미 검증된 안전한 대안이라 이를 표준화. 호칭·이모지는 사용자가 지정한 스타일 기준을 그대로 반영
- 대안으로 고려했던 것: 증언 인용을 가상 인물로 채워 넣는 방안 — 데이터 근거 없이 지어낸 인용이라 기각. "선생님" 호칭을 매 문장에 반복하는 방안 — 부자연스럽고 과잉 반복이라 기각, 세트당 최소 1회로 조정
- 재검증: 호칭·이모지 추가로 늘어난 글자수를 python 실측으로 18건 전량 재검증. 카카오메시지는 전부 200자 이내 유지(이모지 미적용으로 변동 없음). 뉴스레터는 시드 #09(662자)·#16(654자)이 650자 상한을 초과해 문장을 다듬어 각각 638자·627자로 재조정, 나머지는 550~650자 범위 내에서 헤더의 "(n자)" 표기를 실제 글자수로 갱신. 최종 18건 전량 kakao≤200·newsletter 550~650 통과
- 문서화: context/nursevillage-brand-voice.md 신규 작성 — 3가지 규칙 상세 + "증언 인용은 데이터 미비로 이번엔 미적용, 실제 데이터 확보 시 반영 권장"이라는 한계 명시

**[2026-07-01] Standard 산출물 확정 — 발행 캘린더·채널별 핵심 지표**
- 결정 내용(캘린더): output/standard-guide.md에 18건 전체 발행 캘린더 확정. ref_event가 있는 8개 시드(id 01·03·06·07·10·12·15·16)는 "이벤트 발생 전 발행"(발생 후에 접하는 독자가 생기면 안 됨) 원칙에 따라 실제 이벤트 날짜에서 역산한 날짜로 배치:
  - id07(국시 D-30 루틴) → 국시 시행일(2026-01-23) 기준 정확히 D-30 = 2025-12-24
  - id12(국시 끝나고 하고싶은것) → 시행일 D+1 = 2026-01-24 (합격발표 2026-02-13보다 20일 전, 시험 종료~발표 대기 국면 전용이라 발표 전에 노출 필요)
  - id10(SBAR 미리보기) → 합격발표 D-2 = 2026-02-11, id01(프리셉터 마음가짐) → 합격발표 D-1 = 2026-02-12 (둘 다 서술은 "발표 후" 시점이지만, 발행 자체는 발표 전에 걸어 "발표 이후에야 처음 보는 독자"가 없도록 예약 발행)
  - id06(흑역사 모음) → 스승의날(5/15) D-2 = 2026-05-13
  - id03(투약오류 체크루틴) → 세계 환자안전의 날(WHO 확정 9/17) D-3 = 2026-09-14 — 국내 "환자안전주간" 정확한 날짜는 병원별 상이·미확인이라 WHO 확정일을 앵커로 사용
  - id15·16(간호법 시행 관련) → 간호법은 이미 2025-06-21 시행되었고 후속 단계(2026 지정·평가 예비도입 등) 구체 날짜가 미확정이라 역산할 이벤트 날짜가 없음 — 상시 정보형으로 배치(2026-06-10·06-24), 하위법령 확정 등 실제 뉴스 발생 시 발행일 조정 권장
  - ref_event 없는 나머지 10개 상시 콘텐츠는 위 8개 사이 공백을 타깃 생애주기(입사 시즌 3월, 임상실습 시즌 4월 등)에 맞춰 채움
- 결정 내용(지표): 카드뉴스=저장률(정보성 콘텐츠는 "나중에 참고" 목적이 커서 저장이 더 적합한 신호), 카카오채널=클릭률(오픈율은 채널 구조상 이미 높아 변별력 낮음), 뉴스레터=오픈율(국내 실무 관행상 1순위 지표, 단 Apple MPP로 오픈율 집계가 왜곡되는 추세라 클릭률·CTOR을 함께 모니터링)로 확정 — 사용자가 웹 검색으로 확인한 근거를 그대로 반영
- 대안으로 고려했던 것(캘린더): id01·id10을 합격발표 "이후"에 발행하는 방안 — 콘텐츠 서술("발표 후")과는 자연스럽게 맞지만, "발생 후에 접하는 독자가 생기면 안 됨" 원칙에 위배되어 기각. 대신 발표 D-1~D-2 예약 발행으로 절충
- 재현성: 위 날짜는 nursing-calendar.md의 2025~2026 국시 사이클 실제 날짜를 예시로 쓴 연간 반복 템플릿 — 다음 해 재사용 시 국시원·병원 공고로 정확한 날짜를 다시 확인 필요(문서 내 캐비어트로 명시)

**[2026-07-01] 세그먼트별 캐던스(14일) 및 재발행 규칙 도입 — 발행 캘린더 재구성**
- 결정 내용: output/standard-guide.md 발행 캘린더에 "타깃별 최소 14일 캐던스" 원칙을 추가하고, 원본 18건 사이 공백을 재발행 23건("원본id-R1","-R2"...)으로 보완. 재발행 안전장치 3종을 고정: ① 같은 콘텐츠 재사용 최소 70일(2~3개월) 텀 ② 재발행 시 카카오 제목·카드뉴스 1장 헤드라인만 변형(본문 유지) ③ 재발행 항목마다 "재발행 전 법·제도·채용 트렌드 등 최신 정보 변경 여부 확인 필요" 팩트체크 플래그 부착(실제 팩트체크는 이번 범위 밖, 체크리스트 항목으로만 남김)
- 재발행을 허용한 근거: 카카오·SNS 평균 도달률 10~20% 수준(재발행=중복이 아니라 미도달 유저에게 첫 도달), 마케팅 "7회 노출 법칙"(반복 노출 없이는 재방문 행동 전환 안 됨), 이직·채용 시즌마다 신규 유입되는 유저에겐 과거 상시 콘텐츠도 완전히 새로운 콘텐츠라는 점
- 검증 결과(타깃별 최대 공백일, 재발행 반영 후): 신규RN 124일→44일(2026-03-30~05-13 구간 잔존), 간호학생 241일→54일(02-11~04-06 구간 잔존), 경력RN 118일→70일(01-08~03-19 구간 잔존) — **세 타깃 모두 14일 목표는 FAIL**. 원인은 상시(evergreen) 콘텐츠 보유 개수 k와 70일 재사용 텀의 수학적 관계(지속 가능한 평균 발행 주기 하한 = 70÷k일): 신규RN·간호학생은 k=3(평균 23~24일 한계), 경력RN은 k=4(평균 17~18일 한계)라 어느 쪽도 14일에 못 미침. 특히 사이클 초반(해당 타깃의 두 번째 상시 콘텐츠가 아직 70일 재사용 텀을 채우기 전)엔 재발행할 대상 자체가 없어 44~70일의 구조적 공백이 남음
- 대안으로 고려했던 것: 이벤트 앵커 원본 날짜를 조금씩 당겨서 초반 공백을 줄이는 방안 — 이벤트 앵커(예: 국시 시행일, 합격발표일)는 실제 날짜에 고정돼야 하는 값이라 임의 조정은 "발생 전 발행" 원칙과 무관한 왜곡이라 기각. 재발행 텀을 70일보다 짧게(예: 45일) 잡는 방안 — 사용자가 명시한 "2~3개월" 하한을 벗어나 피로도·이탈 리스크가 커진다고 판단해 기각
- 권장사항: 70÷5=14이므로 타깃당 상시 콘텐츠를 최소 5개로 늘리면 이론적으로 14일 캐던스와 70일 재사용 텀을 동시에 만족 가능 — 다음 콘텐츠 기획에서 신규RN·간호학생·경력RN 상시 소재를 각 2개 이상 추가 제작 권장
- 재현성: 재발행 날짜는 "원본 발행일 + 70일 경과 후 가장 이른 슬롯"부터 대상 콘텐츠를 순환 배정하는 고정 규칙으로 산출 — 동일 원본 캘린더가 입력되면 항상 동일한 재발행 일정이 재현됨

**[2026-07-01] 18행 vs 50행 캐던스 재검증 — 데이터 볼륨이 원인이었음을 확인**
- 결정 내용: scripts/check_cadence.py(신규 작성)로 70÷k 공식을 코드화해 타깃별 상시(ref_event 없는) 콘텐츠 개수 k를 재검증. 18행 원본(data/nursevillage_content_seeds_cleaned.csv)은 k=3(신규RN)/3(간호학생)/4(경력RN)로 3개 타깃 전부 FAIL(70÷k=23.3/23.3/17.5일, 목표 14일 초과) — output/standard-guide.md 기존 검증표와 정확히 일치. 반면 50행 더미(data/dummy_seeds_50_cleaned.csv, topic 결측 id50·ref_event 있는 행 제외 후 집계)는 k=13(신규RN)/11(간호학생)/10(경력RN)로 3개 타깃 전부 PASS(70÷k=5.4/6.4/7.0일)
- 결론: 18행 데이터는 구조적으로 14일 캐던스가 불가능하고(k<5), 50행 더미로는 가능함을 확인 — 캐던스 실패의 원인이 로직 결함이 아니라 데이터 볼륨(타깃당 상시 콘텐츠 개수 부족)이었음이 검증됨. standard-guide.md의 권장사항("타깃당 상시 콘텐츠 최소 5개 필요")이 실제 데이터로 재확인된 셈
- 선택한 이유: 가설 검증을 위해 별도 스크립트로 공식을 고정해 재현 가능하게 만들면, 이후 실제 seeds가 늘어날 때마다 같은 명령으로 즉시 PASS/FAIL을 확인할 수 있음(사람이 매번 수기로 표를 다시 그릴 필요 없음)
- 대안으로 고려했던 것: k 집계에 topic 결측 행(id50)·ref_event 있는 시의성 행을 포함하는 방안 — 실제로 생성·순환되지 않는 콘텐츠까지 포함하면 k가 부풀려져 캐던스 여유를 과대평가하게 되므로 기각, 생성 대상에서 제외된 행과 1회성 이벤트 행은 k 집계에서 뺌
- 재현성: `python scripts/check_cadence.py data/nursevillage_content_seeds_cleaned.csv` -> FAIL, `python scripts/check_cadence.py data/dummy_seeds_50_cleaned.csv` -> PASS. 정제 단계부터 재현하려면 `python scripts/run_pipeline.py <seeds_csv> <cleaned_csv>` 한 번으로 정제+캐던스 검증이 순서대로 실행됨(사용법: output/pipeline-usage.md)

**[2026-07-01] Standard 파이프라인 확장 — 발행 이력 로그·신규 아티클 분류·미래 구간 재스케줄링**
- 결정 내용(1단계, data/published_log.csv): output/standard-guide.md의 41건 캘린더(원본 18 + 재발행 23)를 `id,published_date,channel,is_reissue,reissue_of` 스키마로 그대로 옮겨 "지금까지 실제로 나간 것"의 source of truth로 확정. standard-guide.md 자체는 원본 스냅샷으로 두고 건드리지 않음
- 선택한 이유: 재스케줄링 로직이 "과거는 불변, 미래만 재계산"하려면 과거 발행 사실을 원본 캘린더 문서(사람이 읽는 표)가 아니라 기계가 읽기 쉬운 로그로 분리해둬야 함. 이후 실제 운영에서도 "매번 표를 다시 그리지 않고 로그에 append만 하면 되는" 구조를 만들기 위함
- 결정 내용(2단계, data/classified_new_articles.csv): data/dummy_seeds_50_cleaned.csv(48행, 실제 미분류 100개의 스탠드인)를 topic 텍스트만 근거로 target_audience·tone·keywords_required를 독자적으로 재판단. keywords_required는 scripts/clean_seeds.py의 rule1(topic 내 용어사전 매칭)을 재사용해 후보를 뽑고, 부족분은 target_audience별 전형 어휘로 보충. 원본 파일에 이미 있던 값은 참고하지 않고 topic만 보고 판단한 뒤 사후에만 대조함
- 애매했던 판단(3개 이상, 근거 포함):
  1. id 27 "주사 실수 줄이는 법" — topic에 대상 명시가 없음. 실수 방지 콘텐츠는 숙련도가 낮은 시기에 더 필요하다고 보아 신규RN으로 판단했으나, 경력RN 대상(후배 지도용)일 가능성도 배제 못함
  2. id 30/35 "나이트 근무 루틴 브이로그"/"신규 적응기 브이로그" — "브이로그"라는 포맷 자체가 유머형·공감형 어느 쪽도 될 수 있는 중립 포맷이라 톤 판단이 형식이 아닌 소재 해석에 의존함. 가벼운 일상 공유 뉘앙스가 우세하다고 보아 유머형으로 최종 판단
  3. id 31 "국시 합격자 인터뷰" — 국시 합격 시점은 간호학생→신규RN 전환기라 두 타깃 경계에 걸침. "합격자"가 아직 임용 전이라는 점에 무게를 둬 간호학생으로 판단
  4. id 32/39 "교대 근무 적응 꿀팁"/"3교대 노하우 모음" — "꿀팁"·"노하우"라는 어휘가 실제로는 정보형(01-26 배치 구조상 06번)과 유머형(26·32·39) 양쪽에 모두 쓰여, 어휘만으로 톤을 확정할 수 없음을 확인. "모음/리스트" 같은 나열형 구성을 정보형 신호로 보고 최종적으로 정보형 판단
  5. id 33/37 "실습 나가기 전 마인드셋"/"실습 전 챙겨야 할 준비물" — 준비물 나열(정보형 신호)과 실습을 앞둔 심리적 긴장(공감형 신호)이 한 topic 안에 공존. 33은 "마인드셋"(심리 상태 자체가 소재)이라 공감형, 37은 "챙겨야 할"(행동 체크리스트가 소재)이라 정보형으로 갈라 판단
  6. id 40 "간호주간 특별 콘텐츠" — 간호주간은 특정 세그먼트가 아닌 전체 간호사 대상 캠페인이라 target_audience 특정이 원리적으로 어려움. keywords_required(인력기준·처우개선)가 경력RN 관심사에 가까워 경력RN으로 판단했지만 확정적이지 않음
  7. id 48/49 "선배가 알려준 인계 꿀팁 모음" — topic 문자열이 완전히 동일해 target_audience를 topic만으로는 원리적으로 구분 불가능(진짜 구조적 한계, 정답이 없는 케이스). 관습적으로 먼저 등장한 48을 배우는 입장(신규RN), 49를 전달하는 입장(경력RN)으로 임의 배정했고, 실제 운영이라면 작성자 관점 메타데이터가 별도로 필요함을 명시
- 대안으로 고려했던 것: 원본 dummy 파일의 기존 target/tone 값을 그대로 복사 — 이번 과제 취지("직접 판단해서 분류")에 어긋나 기각. 실제로 내 독자 판단 중 id 32/33/37/39는 원본 라벨과 다르게 나왔는데(정보형 vs 유머형/공감형 등), 이는 복붙이 아니라 실제 재판단이 이뤄졌다는 증거로 남김
- 결정 내용(3단계, scripts/reschedule.py -> output/rescheduled-calendar.md): TODAY=2026-07-01 기준으로 published_log.csv를 과거(20건, 불변)/미래(21건, 폐기 후 재계산)로 분리. 18건 원본 + classified_new_articles.csv(id 앞에 `NEW-` 접두어를 붙여 원본 01~18 id와 충돌 방지)를 합쳐 콘텐츠 풀을 재구성하고, scripts/check_cadence.py의 count_evergreen_by_target()/evaluate_cadence()를 그대로 import해 재사용 — 새로 로직을 짜지 않고 기존 파이프라인 함수를 합성만 함
- 재계산된 k값과 캐던스 결과(재현 명령: `python scripts/reschedule.py`): 신규RN k=16(70÷16=4.4일) PASS, 간호학생 k=14(70÷14=5.0일) PASS, 경력RN k=14(70÷14=5.0일) PASS — 3개 타깃 전부 14일 목표 충족(FAIL→PASS 전환 확인, 이전 18행 전용 결과였던 3/3/4 FAIL과 대비됨)
- 이벤트 예외 처리: 아직 발행되지 않은 원본 이벤트 앵커(id 03, 세계 환자안전의 날 D-3=2026-09-14)는 실제 날짜에 고정하고 순환 대상에서 제외. classified_new_articles.csv의 이벤트형 신규 소재 13건(환자안전주간 9건 포함 — 배치 구조상 같은 이벤트에 몰린 더미 데이터 특성)은 실제 캘린더 날짜가 확인되지 않아 자동 배치하지 않고 "미배치 이벤트형 소재"로만 남김(수동 확인 필요)
- 재사용 안전장치: 재발행 후보 선정 시 published_log.csv에서 해당 id의 "마지막 발행일"을 조회해 70일이 지나지 않았으면 후보에서 제외하고 다음 순번으로 넘어가는 로직을 그대로 구현(전에 정한 70일 재사용 텀 원칙 유지)
- 대안으로 고려했던 것: 미래 구간뿐 아니라 41건 전체를 처음부터 다시 스케줄링 — "과거는 이미 실제로 나간 것이라 불변"이라는 이번 과제의 핵심 전제와 어긋나 기각. output/standard-guide.md의 41건 캘린더 자체를 덮어쓰는 방안 — 지시사항("standard-guide.md는 건드리지 마세요")에 따라 기각, 대신 output/rescheduled-calendar.md를 신규 생성해 "오늘 이후 구간만 대체"하는 문서로 분리
- 한 줄 요약: 이번 확장(발행 로그화 → 새 아티클 분류 → 미래만 재스케줄링)은 Standard 8번(재사용 파이프라인) 요건을 "정제+캐던스 검증"에서 "실제 발행 이력을 반영한 지속적 재계산"까지 늘린 연장선이며, 새 스크립트 3종 모두 기존 clean_seeds.py/check_cadence.py의 함수를 그대로 import해 재사용했다

**[2026-07-01] 확정 구간(committed window) 규칙 도입 — scripts/reschedule.py 2차 확장**
- 결정 내용: "고정" 범위를 "TODAY 이전"에서 "TODAY 이전 + TODAY~TODAY+14일(확정 구간)"으로 확대. published_log.csv를 과거(20건)/확정 구간(2건: id 18, 08-R1)/자유 구간(그 이후, 재계산 대상)으로 3분할. 자유 구간 재스케줄링 시작일도 TODAY가 아니라 확정 구간 종료일 다음날(2026-07-16)로 이동. output/rescheduled-calendar.md 섹션 제목을 "1. 확정 구간 — 고정됨(과거 발행분 + 향후 14일)"로 수정
- 선택한 이유: 실제 운영에서는 카피 작성·검수·예약 발행 설정이 발행일 며칠 전에 이미 끝나 있는 경우가 많아, "오늘"만 기준으로 그 이전만 고정하면 내일모레 나갈 콘텐츠까지 재배치 대상이 되어 실무적으로 뒤집기 어려운 일정을 로직이 함부로 바꾸는 문제가 생김. 14일이라는 값은 이번 과제의 "최소 14일 캐던스" 목표 주기와 동일한 길이로 맞춰, "적어도 한 캐던스 주기만큼은 이미 확정된 일정으로 보호한다"는 의미도 겸함
- 재현성 검증: 확정 구간 안 항목(id 18, 08-R1)의 "이전(published_log)"과 "이후(재스케줄링)" 날짜·채널을 output/rescheduled-calendar.md 표로 나란히 출력해 100% 동일(unchanged)함을 확인 — 로직이 이 구간을 아예 건드리지 않는다는 것을 코드가 아니라 산출물로 증명
- 대안으로 고려했던 것: 확정 구간을 7일(주 단위)로 잡는 방안 — 캐던스 목표(14일)와 길이를 다르게 가져가면 "확정 구간 하나가 캐던스 목표를 못 채우는" 혼란이 생길 수 있어 14일로 통일. 확정 구간 없이 TODAY 이전만 고정(기존 방식 유지) — 사용자가 명시적으로 "오늘부터 14일 이내도 고정"을 요구해 기각

**[2026-07-01] 확정 구간 내 신규 시의성 콘텐츠 예외 삽입 규칙**
- 결정 내용: 확정 구간(14일) 안이라도 (1) 실제 이벤트 날짜가 확인되고 (2) 그 날짜까지 오늘 기준 최소 3일(리드타임: 작성 1일 + 검증·수정 1일 + 여유 버퍼 1일) 이상 남아있고 (3) 그 날짜에 이미 고정된 항목이 없는(빈 슬롯) 경우에만 새 시의성 콘텐츠를 예외적으로 끼워 넣도록 구현(scripts/reschedule.py의 evaluate_committed_insertions). 리드타임 조건을 슬롯 조건보다 먼저 검사해, 리드타임 미충족이면 슬롯 상태와 무관하게 즉시 반려
- 실제 이벤트 날짜 확인 원칙: 국내 정확한 날짜가 출처로 확인되지 않는 이벤트(간호주간·코로나19 재유행·면허 갱신 시즌·처우개선 발표)는 절대 날짜를 추정해 넣지 않음(clean_seeds.py·standard-guide.md와 동일한 프로젝트 원칙 유지) — KNOWN_EVENT_DATES에는 세계 환자안전의 날(WHO 9/17 프록시)·스승의날(5/15)·국제 간호사의 날(5/12)처럼 출처가 확인된 것만 등록
- 실행 결과(TODAY=2026-07-01, 확정 구간 2026-07-01~07-15): 위 3개 확인된 이벤트의 "다음 발생일"이 모두 확정 구간 밖(2026-09-17, 2027-05-15, 2027-05-12)이라 이번 실행에서는 삽입 0건. 나머지 4개 이벤트(NEW-40/41/42/43)는 날짜 자체가 미확인이라 애초에 판단 대상이 아니었음 — "삽입 0건"이 로직 결함이 아니라 실제 데이터가 이번 주기엔 해당 사항이 없었기 때문임을 별도로 검증하기 위해 --today를 2026-09-05로 바꾼 합성 시나리오를 스크래치 환경에서 실행: 확정 구간(09-05~09-19) 안에 환자안전주간(09-17)이 들어오자 실제로 삽입 1건(NEW-01, id 오름차순으로 가장 먼저인 후보가 09-17 슬롯을 선점) + 반려 8건(NEW-04/07/10/13/16/19/22/25, 전부 같은 09-17 슬롯을 두고 "슬롯 없음"으로 반려)이 정확히 발생함을 확인(이 검증용 실행은 output/rescheduled-calendar.md에는 반영하지 않고 스크래치에서만 수행 — 공식 산출물은 TODAY=2026-07-01 기준 실제 결과만 담음)
- 대안으로 고려했던 것: 슬롯을 타깃별로 독립 운영(같은 날짜라도 타깃이 다르면 다른 슬롯으로 취급) — published_log.csv의 41건 원본 캘린더가 애초에 "하루 한 슬롯" 원칙으로 설계돼 있어(타깃 관계없이 날짜가 전부 겹치지 않음) 기존 설계와 일관성을 위해 "슬롯=날짜(타깃 무관)"로 채택. 리드타임을 슬롯보다 나중에 검사 — 사용자가 "리드타임 조건이 슬롯 유무보다 우선"이라고 명시해 순서를 그대로 반영

**[2026-07-01] 콘텐츠 생명주기 메타데이터(content_type/expires_after/superseded_by) 도입**
- 결정 내용: data/nursevillage_content_seeds_cleaned.csv(18건)와 data/classified_new_articles.csv(48건)에 content_type(evergreen/recurring_seasonal/time_bound_fact)·expires_after·superseded_by 컬럼 추가. 18건 중 6건(id 01·03·06·07·10·12, 전부 국시·환자안전주간·스승의날처럼 매년 반복되는 이벤트 앵커)을 recurring_seasonal로, 1건(id 16)을 time_bound_fact(만료 2026-12-31)로, 나머지 11건을 evergreen으로 분류. 48건 중 13건(환자안전주간 9건 + 간호주간·코로나19 재유행·면허 갱신 시즌 3건)을 recurring_seasonal로, 1건(NEW-43)을 time_bound_fact(만료 2026-12-31)로, 나머지 34건을 evergreen으로 분류(id 50은 topic 결측이라 판단 대상 제외)
- 판단 기준: 실제 생성된 output/output_content_set.md 본문(18건)을 직접 읽고, "본문이 특정 연도·특정 시행 단계처럼 시간이 지나면 틀려지는 사실을 서술하는가"를 기준으로 time_bound_fact를, "본문 내용은 그대로인데 발행 시점(계절·이벤트)만 매년 갱신하면 되는가"를 기준으로 recurring_seasonal을 판정. 분류 아티클(48건)은 아직 본문이 없어 topic·ref_event만으로 같은 기준을 유추 적용
- 애매했던 판단(4건, 근거 포함):
  1. id 16 "간호법 시행으로 달라지는 현장 포인트" — recurring_seasonal(간호법도 "매년 반복되는 이벤트"로 볼 수 있음) vs time_bound_fact(간호법은 2025-06-21 단발 시행이고, nursing-calendar.md에 따르면 2026년 지정·평가 예비도입·2027년 본격시행 등 후속 단계가 남아있어 본문이 서술하는 "지금의 현장 변화"가 하위법령 확정 시점에 사실관계가 달라질 위험이 큼) 사이에서 고민. 간호법 시행은 매년 반복되는 캘린더 이벤트가 아니라 일회성 제도 변화이므로 recurring_seasonal 정의("매년 반복")에 맞지 않는다고 판단해 time_bound_fact로 최종 확정, 만료일은 하위법령 확정 예상 시점 이전인 2026-12-31로 보수적으로 설정(정확한 하위법령 확정일은 미확인이라 임의값). 같은 ref_event(간호법 시행)를 쓰는 id 15는 병동 평가 체크리스트라는 방법론 자체는 시간이 지나도 유효해 evergreen으로 구분
  2. NEW-31 "국시 합격자 인터뷰" — evergreen(인터뷰라는 포맷 자체는 매년 재사용 가능) vs recurring_seasonal(매년 그 회차의 새 합격자를 인터뷰해야 하므로 텍스트 자체는 매번 완전히 새로 써야 함, 단순 날짜 갱신 수준이 아님) 사이에서 고민. 최종적으로 evergreen 유지(인터뷰이의 특정 인적사항이 본문에 없다면 텍스트 자체는 재사용 가능하다고 보수적으로 판단)했으나, ambiguous_note에 두 분류 모두의 논거를 남겨 실제 생성 단계에서 재판단하도록 유도
  3. NEW-41 "재유행 대비 감염관리 루틴"(ref_event=코로나19 재유행) — recurring_seasonal(감염관리 루틴 자체는 매 유행철 재사용 가능) vs time_bound_fact("코로나19"라는 특정 병원체명이 본문에 박혀 있으면 다음 유행이 다른 병원체일 때 낡아 보임) 사이에서 고민. 현재는 recurring_seasonal로 분류했으나, 실제 생성 시 병원체명을 일반화("감염병 재유행")하면 이 애매함이 해소된다는 점을 ambiguous_note에 남김
  4. NEW-43 "처우개선 발표, 현장 반응은?"(ref_event=간호사 처우개선 발표) — recurring_seasonal("발표"라는 이벤트 유형 자체는 앞으로도 반복 가능) vs time_bound_fact(이 콘텐츠는 특정 시점의 특정 발표에 대한 "현장 반응"을 담고 있어 그 발표가 후속 조치로 대체되면 서술 자체가 틀려짐) 사이에서 고민. 콘텐츠가 "반응"이라는 스냅샷성 정보를 다루는 것이 핵심이라 time_bound_fact로 최종 확정(만료 2026-12-31, id 16과 동일하게 보수적 연말 기준)
- 알려진 한계(설계 노트): 이번 데이터셋의 time_bound_fact 2건(id 16, NEW-43)이 공교롭게 둘 다 ref_event도 함께 갖고 있어, check_cadence.count_evergreen_by_target()/reschedule.py의 상시 순환 풀에는 애초에 들어가지 않는다(ref_event가 있으면 순환 후보에서 제외되는 기존 규칙이 먼저 적용됨). 따라서 "만료된 time_bound_fact를 재발행 후보에서 제외" 로직은 이번 실제 실행(TODAY=2026-07-01, 두 항목 모두 만료 전)에서는 관찰되지 않는다(0건 제외). 로직 자체는 스크래치 환경에서 ref_event 없는 합성 time_bound_fact 항목 3개로 단위 검증해 정상 동작을 확인함(만료된 것만 정확히 제외, 안 만료된 것과 evergreen은 포함)
- 대안으로 고려했던 것: expires_after를 모든 recurring_seasonal에도 채우는 방안 — 스펙상 "expires_after는 time_bound_fact인 경우만" 채우도록 명시돼 있어 recurring_seasonal은 빈칸 유지(대신 재사용 시 갱신 필요성은 content_type 자체로 표현됨). superseded_by를 id 16의 후속 콘텐츠로 임의 지정 — 아직 실제로 작성되지 않은 콘텐츠를 가리킬 수 없어 빈칸 유지, 대신 "하위법령 확정 후 새 아티클 필요"라는 권고만 이 항목에 남김



**[2026-07-01] 삭제된 nursevillage_content_seeds_cleaned.csv 복원**
- 결정 내용: output_content_set.md가 참조하는 nursevillage_content_seeds_cleaned.csv가 커밋되지 않은 상태로 삭제되어 있어 복원함. Basic 산출물 자체는 재검증 결과 이상 없음.
- 선택한 이유: output 파일의 데이터 개요 섹션이 이 파일을 근거 문서로 명시하고 있어, 없으면 제출물의 근거 추적이 끊김
- 대안으로 고려했던 것: 다른 삭제/수정 파일(context/nursing-calendar.md, data/dummy_seeds_50.csv, output/전체보기.md)은 Standard 단계에서 별도로 다루기로 하고 이번엔 건드리지 않음

**[2026-07-01] Standard 산출물 재구성 — 스펙 범위 초과분 제거**
- 결정 내용: 발행 이력 시뮬레이션·미래 재스케줄링·신규 기사 자동분류·재발행 알고리즘(70÷k 기반 확정구간 등)은 Notion 원문 스펙이 요구하지 않는 범위로 판단해 제거함(classified_new_articles.csv, reschedule.py, published_log.csv, rescheduled-calendar.md 등). standard-guide.md·pipeline-usage.md는 재사용 파이프라인 + 발행 캘린더(원본 18건) + 채널별 지표 + 브랜드보이스 가이드로 압축
- 선택한 이유: 스펙(Standard 가산 항목)이 요구하는 것은 "파이프라인 재현성"과 "발행 캘린더·채널별 지표·브랜드보이스 가이드"뿐이며, 그 이상의 시뮬레이션·자동분류·재발행 알고리즘은 채점 범위 밖에서 문서·코드 복잡도만 키우는 과설계로 판단
- 후속 처리: 캐던스 FAIL 인사이트(타깃당 상시 콘텐츠 k값 부족)는 폐기하지 않고 challenge-strategy.md의 전략안 근거 데이터로 이관

**[2026-07-01] output/dashboard.html을 콘텐츠 3종 세트 + KPI 리포트로 교체**
- 결정 내용: output/dashboard.html을 스펙 범위 밖이었던 기존 내용에서 output_content_set.md 18건 전문 + kpi-results.json 4개 지표를 렌더링하는 정적 HTML로 교체(scripts/build_report.py). app.py는 이 파일을 그대로 읽으므로 Streamlit 데모 구조는 변경 없음. 목업 파일(sample-*.html) 3종은 dashboard.html로 대체되어 삭제.
